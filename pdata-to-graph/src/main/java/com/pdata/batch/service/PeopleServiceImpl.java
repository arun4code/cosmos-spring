package com.pdata.batch.service;

import org.springframework.stereotype.Service;

@Service
public class PeopleServiceImpl implements PeopleService {
	//@Autowired
	//PeopleRepo peopleRepo;
	
	@Override
	public void saveTestData() {
		/*
		 * int count = (int) peopleRepo.count(); List<People> appList = new
		 * ArrayList<>(); int total = count + 1000; for (int i = count + 1; i < total;
		 * i++) { appList.add(People.of("firstname" + i, "lastname" + i, i, "email@" +
		 * i)); } peopleRepo.saveAll(appList);
		 */
	}
	
}
